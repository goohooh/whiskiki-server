<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Flavors</title>
    <style>
      * {
        margin: 0;
        padding: 0;
      }
      body {
        padding: 30px 26px;
      }

      h1 {
        margin-top: 30px;
        margin-bottom: 40px;
        text-align: center;
        opacity: 20%;
        font-size: 22px;
        font-weight: 700;
        color: #222;
      }

      li {
        display: flex;
        margin-top: 20px;
        align-items: center;
        list-style: none;
      }
      img {
        margin-right: 10px;
      }

      h2 {
        margin-right: 20px;
        color: #222;
        font-size: 20px;
        font-weight: 700;
      }

      p {
        font-size: 16px;
        font-weight: 700;
        color: #7d7d7d;
      }
    </style>
  </head>
  <body>
    <h1>이런 느낌들을 골라주세요!</h1>
    <ul>
      <% for(let flavor of flavors){ %>
      <li>
        <img width="40" height="40" src="<%= flavor.imageUrl %>" />
        <h2><%= flavor.name %></h2>
        <p><%= flavor.description %></p>
      </li>
      <% } %>
      <li></li>
    </ul>
  </body>
</html>
